# rp2040-micropython-neopixel-spectrum
Audio spectrum visualizer for RP2040 in MicroPython with WS2812B 16×16 NeoPixel matrix output.  
---
Обчислення спектра аудіосигналу та відображення його на RGB LED-матриці **WS2812B 16×16** (NeoPixel) у **MicroPython** на **RP2040 (Raspberry Pi Pico)**.

---

## 📦 Основні характеристики

### Відображення

- Діапазон частот, який відображає індикатор, **не є фіксованим** і визначається параметрами:
  - `Fs` / `SAMPLE_FREQ` — частота семплування (Гц)
  - `N` / `FFT_SIZE` — розмір FFT
  - `IND_BANDS` — розбиття FFT-бінів на смуги (які біни реально візуалізуються)
    
(див. розділ “Розрахунок діапазону частот”)

- **16 частотних смуг** (обмежується LED-матрицю WS2812B)
- **16 рівнів (0..16)** для кожної смуги, масштабування від `dBFS`
- Динамічний масштаб **AGC по смугах** (стабільне візуальне співвідношення складових при зміні гучності)
- Частота оновлення індикації: **~14 FPS** (залежить від параметрів та реалізації; див. розділ “Продуктивність”)

### FFT
- Розмір: **N = 1024**
- Вікно: **Hann window** (вікно Ханна), **Hamming window** (Вікно Геммінга), або без вікна
- Реалізація FFT: **kissFFT (float)** + обчислення енергії в C через модуль `fastfft`  
  Репозиторій: https://github.com/Alex-Teteria/Fast-FFT-module-for-MicroPython-RP2040-

### ADC (RP2040)
- Частота семплування: **Fs = 44 кГц**, Nyquist = 22 кГц
- Захват: C-модуль `adc_dma` (one-shot/burst, апаратне тактування, DMA у RAM)  
  Репозиторій: https://github.com/Alex-Teteria/MicroPython-RP2040-ADC-DMA-Extension
- Окремий аналоговий антиаліасовий фільтр не застосовано; вхідний тракт (електретний мікрофон + MAX4466) призначений для аудіо й має обмежену корисну смугу. Для зменшення впливу можливих ВЧ артефактів індикатор обмежує відображення до ~18 кГц. За наявності ВЧ перешкод можливий аліасинг; у такому разі рекомендується простий RC low-pass перед ADC.

---

## 🏗️ Апаратна частина та підключення

### Модулі
- MAX4466 electret mic amplifier module (мікрофон + підсилювач)
- WS2812B RGB LED matrix 16×16 (256 LED)
- Raspberry Pi Pico / RP2040

### Живлення та сигнали
- MAX4466 живити від **3.3V** Pico.
- Вихід MAX4466 має **DC offset ~VCC/2**, тому може подаватись напряму на ADC.
- LED-матрицю WS2812B живити від **5V окремим джерелом** (струм залежить від яскравості та кольорів).
- Обов’язково зробити **спільну землю (GND)** між Pico та 5V LED-джерелом.
- DATA лінія WS2812B:
  - підключити послідовно резистор **330–470 Ω** у DATA біля входу матриці.

### Піни 
- ADC: GPIO26 / ADC0
- NeoPixel DATA: GPIO20 (відповідає `neo_pin=20` у коді)

---

## 🧩 Встановлення / прошивка

Проєкт використовує модулі `adc_dma` та `fastfft`. Потрібна збірка MicroPython для RP2040, де ці C-модулі вшиті у прошивці.

Далі:
1. Завантажити на Pico файли:
   - `neo_spectr.py`
   - `neo_matrix.py`
   - `build_band_spectr.md` (довідка)
2. Запустити `neo_spectr.py`.

---

## :page_facing_up: Призначення файлів

| Файл | Призначення |
| --- | --- |
| `neo_spectr.py` | Основний код (ADC → FFT → 16 смуг → LED) |
| `neo_matrix.py` | Клас `NeoMatrixFast` для швидкого рендеру WS2812B 16×16 (viper + прямий буфер) |
| `build_band_spectr.md` | Опис алгоритму та параметрів функції `build_band_spectr()` |

---

## ⚙️ Ключові параметри

- `FFT_SIZE = 1024`
- `SAMPLE_FREQ = 44_000`
- `IND_BANDS` — розбиття бінів FFT на 16 смуг
- `NOISE_THRESHOLD` — пороги шуму по смугах
- AGC параметри: `HEADROOM_DB`, `SCALE_DECAY_DB`, `SCALE_MIN_DB`, `GAMMA`

---
## :triangular_ruler: Розрахунок діапазону частот 


#### Крок по частоті (роздільна здатність FFT)
Для real FFT з розміром `N` частотний крок одного біну:

$$
\Delta f = \frac{Fs}{N}
$$

#### Відповідність "бін → частота"
Частота біну з індексом `k`:

$$
f(k) = k \cdot \Delta f = k \cdot \frac{Fs}{N}
$$

де:
- `k = 0` — DC (0 Гц), не візуалізується
- верхня межа спектра обмежена частотою Найквіста:

$$
f_{Nyq} = \frac{Fs}{2}
$$

#### Діапазон відображення
Якщо фактично візуалізуються біни від `k_min` до `k_max` (включно), то приблизний діапазон відображення:

$$
f_{min} \approx k_{min} \cdot \frac{Fs}{N}, \quad
f_{max} \approx k_{max} \cdot \frac{Fs}{N}
$$

Значення `k_min` і `k_max` визначаються реальною областю бінів, яку покриває `IND_BANDS`
(тобто найменший/найбільший бін, що входить у будь-яку смугу).

#### Приклад
Для `Fs = 44_000` і `N = 1024`:

$$
\Delta f \approx \frac{44\,000}{1024} \approx 43.07 \text{ Гц}
$$

Якщо `IND_BANDS` охоплює біни `k_min = 1` … `k_max = 418`, тоді:
- $f_{min} \approx 1 \cdot 43.07 \approx 43$ Гц
- $f_{max} \approx 418 \cdot 43.07 \approx 18.0$ кГц

> Примітка: реальні межі можуть відрізнятись залежно від того, які саме біни/смуги включені у `IND_BANDS`.


---



## 📈 Продуктивність 

Кадр включає:
- час захвату: `FFT_SIZE / Fs` (для 1024/44k ≈ 23.27 ms)
- FFT (для N=1024 + вікно ≈ 35 ms)
- агрегація смуг + AGC (≈ 12 ms)
- вивід WS2812B: `np.write()` (для 256 LED типово ~7–8 ms)

Фактичний FPS залежить від:
- реалізації `fastfft`
- параметрів AGC/логарифмів
- яскравості/оновлення LED та структури потоку виконання  

У проєкті використано 2 ядра RP2040:  
- **Core0**: захват ADC (`FFT_SIZE/Fs`), FFT, агрегація смуг, AGC, підготовка `spec_back/max_back` і публікація кадру (swap).
- **Core1**: рендер буфера (`viper`) та вивід на матрицю `np.write()` (WS2812B).

Тому `np.write()` **не збільшує латентність DSP-кадра на Core0** у режимі `DROP_FRAMES=True`,
але обмежує **максимальну частоту оновлення індикації** (пропуск кадрів можливий, якщо Core0 генерує кадри швидше, ніж Core1 встигає їх показувати).  
У режимі `DROP_FRAMES=False` Core0 очікує, доки Core1 забере кадр, і тоді `np.write()` напряму обмежує частоту всієї системи.

---

## 🔧 Рекомендації (без AA-фільтра)
Якщо у верхній частині спектра помітні “дивні” піки/шум:
- обмежити відображення до 12–16 кГц через `IND_BANDS`
- додати простий RC low-pass перед ADC (1-й порядок):
  - приклад: R=1 кΩ послідовно, C=10 нФ на GND (fc ≈ 16 кГц)

---

## 📄 Credits and License
- Licensed under MIT.
- **kissFFT**: This project uses the [kissFFT](https://github.com/mborgerding/kissfft) library by Mark Borgerding, licensed under the **BSD 3-Clause License**.
